import { View, Text } from 'react-native';

export default function ProfileTab() {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>Profile — Phase 3</Text>
    </View>
  );
}
